# Страница «MVP · Ready for Dev» — передача техдиру

Дата: 17.08.2026. Основание: `MVP.md` в корне репозитория (английский, для CTO),
`CLAUDE.md` §2 (состав MVP).

---

## Итог дня: два решения основателя, второе отменило первое

**Утром:** «визуальная правда — прототип `web/`, перерисуй Figma под него, но на
новой странице-дубликате, оригинал остаётся у нас». Сделано: 44 экрана в стиле shadcn,
токены сняты из `globals.css`, размеры измерены `getComputedStyle` на работающем прототипе.

**Вечером:** «сделай дизайн 1 в 1 как на странице V2.0, мне он больше нравится».
Страница пересобрана в **Forest & Lime**. Вариант в стиле shadcn не удалён, а отложен
на страницу **`MVP · shadcn-Variante (abgelegt)`** — удалить одним движением, если не нужен.

**Следствие, которое нельзя замолчать:** прототип `web/` теперь расходится с эталоном.
Его поведение, маршруты, формы данных и модель редактирования остаются в силе, но
**оформление придётся приводить к Figma** — это записано в `MVP.md` как риск R6 и как
работа в M4. Из прототипа переживает всё, кроме темы: имена токенов, четыре цвета бренда
и каждое поведенческое решение.

---

## Что на странице

**`MVP · Ready for Dev` `675:5792`** в `X5cmjr1EwurV8FrkN6BYxc`:
**47 экранов — 22 Desktop (1440 × 1024), 25 Mobile (375 × 812)**, три секции,
блок «Read me first». Изначально было 44; три добавлены 17.08 — см. дополнение в конце.

**Открытие, объясняющее выбор основателя:** `Screens V2.0` `309:76` — это **ровно
объём MVP**. 20 десктопных и 24 мобильных экрана, семь пунктов меню, ни одного экрана
Phase 2. V3 добавила четыре пункта в сайдбар, строки вкладок и 38 экранов будущих волн.
То есть V2 нравится не только внешне — она честнее по объёму.

**Как собрано.** Не переводом 788 русских строк из V2, а **клонированием MVP-блока
из `Screens V3.0`**: там те же экраны, уже полностью по-немецки и на компонентах.
V3 отличается от V2 ровно тремя вещами, и все три откачены:

| Что | Как вернули к V2 |
|---|---|
| Сайдбар вырос до 11 пунктов | Копия компонента на странице MVP, 4 варианта Phase 2 удалены, из семи оставшихся вырезано 28 пунктов. **Оригинал на V3 не тронут** |
| Обзор переставлен: тёмная полоса «Fund» сверху | Полоса убрана, панель «Preiskontrolle» клонирована из V2 `309:367` и поставлена справа от «Letzte Belege» (756 + 360) |
| Строки вкладок с адресами Phase 2 | Удалены три на десктопе (Analyse, Lieferanten, DATEV-Export), пункт **WhatsApp** из подменю настроек на пяти экранах, чип-строка «Bereichswechsel» на мобильной аналитике |

Проверка после сборки: **0 кириллицы, 6 переполнений** (три — декоративный
эллипс, который панель обрезает намеренно; два поправлены сдвигом; одно внутри
auto-layout). Средние точки `·` в подписях («AUSGABEN · JUNI») оставлены намеренно:
`AI_rules.md` §8 их запрещает, но прямая просьба «1 в 1 как V2» по собственной иерархии
этого файла старше правила.

---

## Язык

**Весь метаслой — английский**, его читает разработчик: 10 аннотаций Dev Mode на экранах
плюс одна на сайдбаре, блок «Read me first», имена секций и подписи рядов.
**Немецким остаётся содержимое экранов** — это интерфейс продукта.

Аннотации несут не описание вида, а правила сборки: почему сравнение цен всегда идёт
через базовую единицу (ошибка прототипа «+424 %»), почему дубликат ловится по
«поставщик + номер + брутто», а не по файлу, почему пороги — настройки арендатора,
почему анкета онбординга обязана быть в MVP.

---

## ⚠️ Осталось сделать руками

**Статус «Ready for dev» через Plugin API не пишется** — `devStatus` в песочнице закрыт
и на чтение, и на запись (проверено дважды). Нужно: **Dev Mode → выделить секции
«MVP · Desktop» и «MVP · Mobile» → Ready for dev.** Два клика. Это же написано на самой
странице.

---

## Ловушки Figma API, которые сработали

1. **`get_metadata` без `nodeId` вернул одну страницу из двенадцати.** Список неполный —
   на нём чуть не построили вывод, что страницы удалены. Проверять через
   `figma.root.children`.
2. **`appendChild` не возвращает узел** — `add(parent, node).layoutSizing…` падает на `null`.
3. **`console.log` из `use_figma` не доходит** — только `return`.
4. **Переименование в цикле по `findAll`** ломается, если ключ ищется по уже изменённому
   имени: сначала собрать пары, потом присваивать.
5. **`combineAsVariants` требует имён `Свойство=Значение`** — иначе свойство остаётся
   безымянным и варианты читаются как `=camera`.
6. **Сумма ширин колонок + `itemSpacing × (n−1)` = ширина карточки − 2 × padding.**
   Промах на 48 px в шести таблицах.
7. **Шапка таблицы: ширина − 10, `itemSpacing` + 10** — начала колонок на месте,
   заголовки не слипаются.
8. **`x`/`y` внутри auto-layout игнорируются** — иконку в кружок надо вкладывать.
9. **`devStatus` недоступен, `annotations` доступны.**

---

См. [[prototyp-shadcn]], [[ui-v3-components]], [[ui-phase2-screens]], [[roadmap]].

---

## Дополнение 17.08.2026 — пять лёгких фич и роль Steuerberater

Основатель: «давай сделаем это в MVP». Принцип «MVP не раздувается» нарушен **сознательно**,
и вот чем это оправдано: **ни одна из пяти не зависит от того, насколько хорошо читает модель.**
Они работают на структурированных E-Rechnung и на арифметике. Если риск R1 сработает — мятый
чек Metro не читается — продукт всё равно делает то, за что платят. Цена страховки ≈ 2,5 недели,
план вырос с 12 до 14.

**Три теста, которые не пускают раздуваться дальше:** работает на первом документе (иначе на демо
пустой экран) · детерминированная (нет новой поломки) · **не добавляет типа документа** — именно
этот тест оставил Vertrags-Radar, абонементы и энергию в волне D, хотя поштучно они дёшевы.

| Фича | Где в плане | Почему дёшево |
|---|---|---|
| Сроки оплаты и Skonto (идеи 3+14) | поля в M2, экраны в M5 | условия оплаты — текст в счёте, тот же парсер |
| Защита от двойной оплаты как деньги (9) | M4 | правило уже есть, добавляется формулировка и событие |
| Сравнение поставщиков по артикулу (срез 1/11) | M4 | запрос по маппингу, который M4 и строит |
| Еженедельное письмо (13 без WhatsApp) | M7 | почтовый контур там и так делается |
| Роль Steuerberater | M6 | роль + набор прав |

**⚠️ Честное ограничение по Skonto:** скидка реализуется, только если платят раньше, а это упирается
в кассовый разрыв. Показывать «43,93 € ещё достижимо» и «первый срок истекает завтра» — можно.
Писать «сэкономлено» до факта оплаты — нельзя. Записано в аннотацию экрана.

### Роль Steuerberater — read + export, никогда не edit и не buchen

Read-only бесполезен: работа канцелярии начинается там, где заканчивается наша. Может: смотреть
документы и оригиналы, **собрать и скачать DATEV-стапель**, CSV и GoBD Z3, задать Rückfrage.
Не может: править, буховать, трогать тариф, биллинг и пользователей.

**Почему проводка остаётся у ресторана.** Экран проверки — это место, где учится маппинг, а знает,
что «RIND HACK 5KG FRISCH» это Rinderhackfleisch, человек на кухне, а не в канцелярии. Отдать
проводку бухгалтеру = каталог перестаёт учиться, история цен не наполняется, и все алерты, которыми
мы продаём продукт, замолкают. Плюс аудит-трейл чище, когда у проводки один владелец.

**Экспорт обязан нести Belege.** CSV из одних чисел заставляет канцелярию цеплять документы руками —
ровно ту работу, которую мы обещаем убрать. Поэтому ZIP: `EXTF_Buchungsstapel.csv` (заголовок 700,
спека DATEV 1034038) + оригиналы по номеру документа + манифест связи.
**Проверить по спеке до сборки:** может ли запись нести ссылку на изображение (`Beleglink`/BEDI),
которую DATEV разрешает в Belegbild. Sandbox и Prüftool (Hilfe-Center 1070393) открыты без
партнёрства — вопрос закрывается за вечер, место ему в M5.

**Портал остаётся в Phase 2.** Роль на одном тенанте — не второй продукт, портал на много мандантов —
второй. У клонированного экрана `Kanzlei-Portal` левая колонка со списком из шести мандантов удалена.

### Правки в существующих документах

- `CLAUDE.md` §2.5: роли **Owner / Manager / Staff / Steuerberater** (было три).
- `MVP.md`: §2.1 пункты 12–15, новый §2.1a (обоснование и три теста), §7a (роль), §8a (состав
  экспорта), объём экранов 44 → **47**, план M1–M7 12 → 14 недель.
- Страница Figma: **+3 экрана** — `Fälligkeiten` (desktop, mobile) и `Kanzlei-Ansicht` (только
  desktop, бухгалтер работает за столом). `Fälligkeiten` — **вкладка под Belege, а не восьмой пункт
  сайдбара**; строки вкладок Phase 2 на обоих экранах заменены. Ещё две фичи экранов не требуют:
  защита от двойной оплаты — это формулировка на существующей находке, сравнение поставщиков —
  панель внутри `Katalog`.

---

## User Flow и ревизия покрытия — 17.08.2026

Страница **`User Flow · MVP`** в том же файле: семь сценариев, каждая плитка — шаг, который
пользователь реально делает. Белая = экран есть, **пунктирная оранжевая = шаг в сценарии есть,
а экрана или состояния под него нет**. Смысл упражнения не в схеме, а в том, что дыры видно
только когда выстраиваешь путь целиком.

**Найдено 7 дыр.** Инвентарь при этом сходится: 22 + 25 = 47 экранов, как в `MVP.md`.
То есть пропущены не экраны из списка, а шаги, которых в списке никогда и не было.

| # | Дыра | Где | Насколько серьёзно |
|---|---|---|---|
| 1 | **Storno гебухованного белега** | сценарий 6 | **Самая тяжёлая.** `CLAUDE.md` §5.3 запрещает жёсткое удаление: только soft-delete + Storno. Требование записано, интерфейса нет вообще. Ошибочно проведённый документ сейчас исправить нечем |
| 2 | **Hochladen на десктопе** | сценарий 2 | На мобильном лист есть, на десктопе кнопка «Beleg erfassen» ведёт в никуда. Метро-чеки фотографируют телефоном, но PDF и XML приходят на компьютер |
| 3 | **Несколько белегов на одном фото** | сценарий 2 | Разделение заявлено в `MVP.md` §2.1 п. 2. Пользователь обязан увидеть «распознали 3 накладные» и подтвердить — иначе split происходит молча |
| 4 | **Состояние «идёт обработка»** | сценарий 2 | Пайплайн асинхронный (Celery). Самый частый переход в продукте — «снял → подождал → результат» — не нарисован. Сюда же неудачное извлечение и dead-letter |
| 5 | **Панель сравнения поставщиков** | сценарий 3 | Обещана в `MVP.md` §2.1 п. 14 как панель внутри `Katalog`, в макете её нет |
| 6 | **Rückfrage от канцелярии у владельца** | сценарий 6 | Введена в §7a. Бухгалтер задаёт вопрос — владелец должен его увидеть в очереди проверки |
| 7 | **Кончился пакет документов** | сценарий 7 | 200 из 200 наступит у каждого платящего клиента. Биллинг в MVP, а этого состояния нет |

**Что из этого экраны, а что состояния.** Настоящие новые экраны — только 1, 2 и 3
(Storno, Hochladen на десктопе, подтверждение split). 4, 6 и 7 — состояния существующих
экранов, 5 — панель внутри `Katalog`. То есть цена закрытия всех семи ≈ три экрана плюс
четыре состояния, а не семь экранов.

**Почему ревизия сработала.** Проверка по списку экранов ничего бы не дала: список полон
и совпадает со спекой. Дыры видны только при выстраивании пути — Storno отсутствует не потому,
что кто-то забыл нарисовать экран из списка, а потому что в списке его не было изначально,
хотя правило GoBD записано в `CLAUDE.md` с самого первого дня.
